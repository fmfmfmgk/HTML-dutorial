script에서 html요소로 접근 방법

id를 이용한 방법
var a = document.getElementById('id이름')
var a = document.querySelector('#id이름')

class를 이용한 방법
var b1 = document.getElementsByClassName('class이름')
var b1 = document.querySelectorAll('.class이름')
b1[0].innerHTML
b1[1].innerHTML

//여러개중에서 첫번째것만 가져옴
var b2 = document.querySelector('.class이름')
b2.innerHTML


name을 이용한 방법
var c = document.getElementsByName('name이름')
c[0].
c[1].

tag이름을 이용한 방법
d1 = document.getElementsByTagName('tag이름')
d1 = document.querySelectorAll('tag이름')

d2 = document.querySelector('tag이름')


//요소의 값을 가져오기 ************************
a.value         -input요소,select/option, textarea
b1[0].innerHTML -div, p, span, td, label, a, h1~h6, b, strong, em, mark
d2.innerText    -div, p, span, td, label, a, h1~h6, b, strong, em, mark

<p>hello <span>hi~~</span> <a>안녕하세요</a> </p>

//요소의 결과값을 출력하기
a.value = "hi"; -input요소,select/option, textarea
b1[0].innerHTML = "<a>하하하</a>"; 
b1[1].innerText = "<a>하하하</a>"; 































